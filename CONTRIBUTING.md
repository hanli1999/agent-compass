# Agent Compass Development

1. Create a virtual environment.
2. Install `python -m pip install -e '.[dev]'`.
3. Keep tests offline and use synthetic fixtures.
4. Run `python -m pytest` before submitting.
5. Never commit private transcripts, real paths, credentials, or account IDs.
6. Add a schema version when changing a public record.
